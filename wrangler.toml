name = "web3-toolkit"
compatibility_date = "2025-01-27"
pages_build_output_dir = "dist"


# Cloudflare Pages 配置
# 部署时会自动检测 Vite 项目并使用正确的构建命令

# 环境变量（如需要）
# [vars]
# VITE_API_URL = "https://api.example.com"
